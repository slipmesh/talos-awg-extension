# Passed straight through as --build-arg=VERSION=..., computed by the Makefile as
# AWG_SHORT-TALOS_VERSION-DAEMONS_SHA, rather than reassembled here from other build-args -
# simpler than the string manipulation zfs/vars.yaml does for the same purpose.
VERSION: "{{ .BUILD_ARG_VERSION }}"
