# A Talos system extension is just an image with a manifest and a rootfs - no build
# system involved. This is the same three lines the working hand-built extension used.
FROM scratch
COPY manifest.yaml /manifest.yaml
COPY rootfs /rootfs
